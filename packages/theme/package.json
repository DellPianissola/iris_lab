{
  "name": "@nomai/theme",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Paleta → tokens → CSS. Onde moram as opiniões de produto sobre cor.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nomai/color": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "vitest": "^3.2.4"
  }
}
